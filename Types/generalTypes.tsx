export interface FetchedItems {
  full_name: string;
  owner: { avatar_url: string };
  description: string;
}

export interface Item {
  full_name: string;
  avatar_url: string;
  description: string;
}
